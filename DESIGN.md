# 대한민국 주요 경제·정책 이벤트 캘린더 — 설계서 v0.2

> 상태: **C안(서버리스형) 채택 후 Phase 1 구현 완료.**
> 이 문서는 "왜 이렇게 만들었는가"를 남기는 설계 기록입니다.
> 실행 방법과 설치 절차는 [README.md](README.md), 코드 작업 규칙은 [CLAUDE.md](CLAUDE.md)를 보세요.

> 목적: 정부·중앙은행·국회 등에서 발생하는 **날짜가 확정되어 있거나 예고되는 이벤트**를 한 곳에 모아, 표(리스트)와 달력(캘린더) 두 가지 뷰로 확인하고, 자동 또는 수동으로 갱신할 수 있는 프로그램.

---

## 1. 배경 및 목표

### 1.1 문제 정의
- 기준금리 결정, 물가·고용 지표 발표, 부동산 대책 발표, 국정감사 등 시장·정책에 영향을 주는 일정이 **기관별로 흩어져 있음**
- 각 기관이 제공하는 형식이 제각각(HTML 공지, PDF, 엑셀, OpenAPI, RSS)이라 수작업 취합 부담이 큼
- "이번 주에 뭐가 나오지?", "다음 금통위 언제지?"에 즉시 답할 수 있는 뷰가 없음

### 1.2 목표
| 구분 | 목표 |
|---|---|
| 1차(MVP) | 주요 8~10개 소스의 일정을 수집 → 표 + 월간 캘린더로 표시, 수동 갱신 버튼 |
| 2차 | 스케줄러 기반 자동 갱신, 일정 변경 감지, 필터/검색, ICS 내보내기 |
| 3차 | 알림(D-1, 당일), 발표 실적치 연동, AI 요약 브리핑 |

### 1.3 비목표 (Out of scope)
- 투자 추천·매매 신호 제공 (❌)
- 지표 값 자체의 예측 모델링 (초기 버전 제외)
- 다국어 지원 (초기 한국어만)

---

## 2. 대상 이벤트 카테고리

| 카테고리 | 코드 | 주요 이벤트 예시 | 주 출처 |
|---|---|---|---|
| 통화정책 | `MONETARY` | 금융통화위원회(기준금리), 통화신용정책보고서, 경제전망 | 한국은행 |
| 경제지표 | `INDICATOR` | 소비자물가동향, 고용동향, 산업활동동향, GDP 속보치, 국제수지 | 통계청, 한국은행 |
| 부동산 | `REALESTATE` | 주택공급 대책, 청약 일정, 공시가격 발표, 전월세 통계, 규제지역 지정 | 국토교통부, 한국부동산원 |
| 재정·예산 | `FISCAL` | 경제정책방향, 세법개정안, 예산안 제출/심사, 추경 | 기획재정부 |
| 국회 | `ASSEMBLY` | 본회의, 상임위, 국정감사, 대정부질문, 청문회 | 국회 의사일정 |
| 금융·감독 | `FINANCE` | 금융위 정례회의, DSR·LTV 규제 시행일, 스트레스테스트 | 금융위원회, 금감원 |
| 시장 일정 | `MARKET` | 증시 휴장일, 선물·옵션 만기, 배당락, 공모주 일정 | 한국거래소 |
| 해외(참고) | `GLOBAL` | FOMC, 미 CPI·고용지표, ECB, 일본은행 | (선택) |
| 사용자 정의 | `CUSTOM` | 직접 입력한 일정 | 수동 |

> 💡 카테고리별 색상을 캘린더에 매핑 (예: 통화정책=적색, 부동산=녹색, 국회=보라)

---

## 3. 기능 명세

### 3.1 조회 기능

**F-01 캘린더 뷰**
- 월간 / 주간 / 리스트(어젠다) 뷰 전환
- 셀 안에 카테고리 색상 점 + 이벤트 제목(축약), 3개 초과 시 "+N개"
- 오늘 날짜 하이라이트, 공휴일 표시
- 이벤트 클릭 → 우측 상세 패널(Drawer)

**F-02 표(테이블) 뷰**
- 컬럼: 날짜 / 요일 / D-day / 카테고리 / 기관 / 이벤트명 / 중요도 / 확정여부 / 원문링크
- 정렬(날짜·중요도), 페이지네이션 or 무한 스크롤
- 다중 필터: 카테고리, 기관, 중요도(★1~3), 기간, 확정/잠정
- 키워드 검색 (제목·설명 대상)

**F-03 상세 패널**
- 이벤트 설명, 발표 시각(예: 08:00 공표), 관련 직전 발표치, 원문 URL, 수집 시각, 변경 이력

**F-04 대시보드(홈)**
- "이번 주 주요 일정" 카드 (중요도 ★★★ 위주)
- 다음 금통위 D-day 위젯
- 최근 7일 내 일정 변경 알림 배지

### 3.2 갱신 기능

**F-10 수동 갱신 (필수)**
- 상단 `↻ 갱신` 버튼 → 전체 소스 재수집
- 소스별 개별 갱신 버튼 (한국은행만 다시 긁기 등)
- 진행 상황 표시: 소스명 / 성공·실패 / 신규 N건 / 변경 M건
- 마지막 갱신 시각을 항상 화면에 노출

**F-11 자동 갱신**
- 스케줄러(cron) — 기본 매일 07:00, 19:00 (KST)
- 소스별 개별 주기 설정 가능 (국회 의사일정은 매일, 통계청 연간 공표일정은 주 1회)
- 실패 시 지수 백오프 재시도(최대 3회) + 실패 로그 기록

**F-12 변경 감지 (Diff)**
- 이벤트 고유키 기준 비교 → `신규 / 일정변경 / 취소 / 내용수정` 판별
- 변경 이력 테이블에 before/after 저장
- 캘린더에서 변경된 이벤트에 뱃지 표시

**F-13 수동 입력·보정**
- 이벤트 직접 추가 / 수정 / 삭제 (`CUSTOM` 또는 수집분 오버라이드)
- 수집 결과가 잘못됐을 때 `수동 확정` 플래그를 걸면 이후 자동 수집이 덮어쓰지 않음
- CSV / 엑셀 일괄 업로드

### 3.3 부가 기능

| ID | 기능 | 우선순위 |
|---|---|---|
| F-20 | ICS(iCalendar) 내보내기 → 구글/애플 캘린더 구독 | 높음 |
| F-21 | CSV·엑셀 내보내기 | 중간 |
| F-22 | 즐겨찾기(북마크) 및 즐겨찾기만 보기 | 중간 |
| F-23 | 알림 (브라우저 푸시 / 이메일 / 슬랙 웹훅) D-1, D-0 | 중간 |
| F-24 | AI 주간 브리핑 자동 생성 ("이번 주 3대 관전 포인트") | 낮음 |
| F-25 | 발표 후 실제 수치 자동 채우기 (기준금리 3.00% 동결 등) | 낮음 |
| F-26 | 다크모드 / 인쇄용 레이아웃 | 낮음 |

---

## 4. 데이터 모델

```jsonc
// event
{
  "id": "bok-mpc-2026-08-27",          // 결정적(deterministic) ID
  "dedupe_key": "BOK|MPC|2026-08-27",  // 중복 판정 키
  "title": "금융통화위원회 (통화정책방향)",
  "category": "MONETARY",
  "agency": "한국은행",
  "date": "2026-08-27",
  "end_date": null,                    // 다일 이벤트(국정감사 등)
  "time": "09:00",                     // 없으면 null → 종일 이벤트
  "importance": 3,                     // 1~3
  "status": "CONFIRMED",               // CONFIRMED | TENTATIVE | CANCELLED
  "description": "기준금리 결정 및 총재 기자간담회",
  "source_url": "https://...",
  "source_id": "bok_schedule",
  "tags": ["기준금리", "통화정책"],
  "locked": false,                     // 수동 확정 시 true → 자동수집이 덮어쓰지 않음
  "first_seen_at": "2026-01-05T07:00:00+09:00",
  "last_seen_at": "2026-07-20T07:00:00+09:00",
  "updated_at": "2026-07-20T07:00:00+09:00"
}
```

**보조 테이블**

- `source` : 소스ID, 이름, 기관, 수집 방식(API/HTML/RSS/파일), 엔드포인트, 주기, 활성여부, 마지막 성공 시각
- `run_log` : 실행ID, 소스ID, 시작·종료, 상태, 신규/변경/삭제 건수, 에러 메시지
- `change_log` : 이벤트ID, 변경 유형, 변경 필드, before, after, 감지 시각
- `holiday` : 공휴일·증시 휴장일 (별도 관리)

---

## 5. 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│  Frontend (React + TS)                                  │
│   Dashboard │ Calendar View │ Table View │ Admin/Sources │
└───────────────▲─────────────────────────────────────────┘
                │ REST/JSON
┌───────────────┴─────────────────────────────────────────┐
│  Backend API                                            │
│   GET  /events?from&to&category&q                       │
│   POST /events            (수동 추가)                    │
│   PATCH/DELETE /events/{id}                             │
│   POST /refresh          (전체 수동 갱신)                 │
│   POST /refresh/{sourceId} (개별 갱신)                   │
│   GET  /sources, /runs, /changes                        │
│   GET  /export.ics , /export.csv                        │
└───────────────▲─────────────────────────────────────────┘
                │
┌───────────────┴─────────────────────────────────────────┐
│  Collector Layer  (소스별 Adapter, 공통 인터페이스)        │
│   BokAdapter │ KostatAdapter │ MolitAdapter │ ...        │
│      ↓ fetch → parse → normalize → validate             │
│   Normalizer → Deduper → Differ → Persist               │
└───────────────▲─────────────────────────────────────────┘
                │
        Scheduler (cron) / Manual Trigger
                │
            Storage (SQLite or PostgreSQL)
```

### 5.1 Adapter 인터페이스 (핵심 확장 포인트)

```ts
interface SourceAdapter {
  id: string;
  name: string;
  agency: string;
  schedule: string;              // cron 표현식
  fetch(): Promise<RawPayload>;  // HTTP/API/파일
  parse(raw: RawPayload): RawEvent[];
  normalize(e: RawEvent): Event; // 공통 스키마로 변환
}
```
> 새 기관 추가 = Adapter 파일 1개 추가 + 레지스트리 등록. 나머지 파이프라인은 수정 불필요.

### 5.2 파이프라인 규칙
1. **Fetch** — 타임아웃 10초, User-Agent 명시, robots.txt 존중, 요청 간 딜레이
2. **Parse** — 실패 시 해당 소스만 실패 처리(전체 롤백 아님), 원본 스냅샷 보관
3. **Normalize** — 날짜 문자열 통일(`2026. 8. 27.(수)` → `2026-08-27`), 기관명 표준화
4. **Validate** — 필수 필드 존재, 날짜 유효성, 과거 3년~미래 2년 범위 체크
5. **Dedupe** — `dedupe_key` 기준 upsert
6. **Diff** — 기존 레코드와 비교해 change_log 기록, `locked=true`는 스킵
7. **Persist** — 트랜잭션 단위 커밋

---

## 6. 데이터 소스 후보

| 소스ID | 기관 | 내용 | 방식 | 난이도 |
|---|---|---|---|---|
| `bok_schedule` | 한국은행 | 주요 행사·공표 일정 | HTML/OpenAPI(ECOS) | 중 |
| `kostat_release` | 통계청 | 연간 공표일정 (KOSIS) | 엑셀/OpenAPI | 하 |
| `moef_press` | 기획재정부 | 보도자료·발표계획 | HTML/RSS | 중 |
| `molit_press` | 국토교통부 | 부동산 대책·보도자료 | HTML/RSS | 중 |
| `assembly_agenda` | 국회 | 의사일정 (열린국회정보 API) | OpenAPI | 하 |
| `fsc_notice` | 금융위원회 | 정례회의·규제 시행일 | HTML | 중 |
| `krx_calendar` | 한국거래소 | 휴장일·만기일 | HTML/파일 | 하 |
| `data_go_kr` | 공공데이터포털 | 공휴일 정보 API 등 | OpenAPI | 하 |
| `manual` | — | 사용자 직접 입력 | — | — |

> ⚠️ **운영 원칙**: 공식 OpenAPI가 있으면 무조건 API 우선. HTML 파싱은 최후 수단이며, 이용약관·robots.txt를 확인하고 과도한 요청을 하지 않음. 각 소스마다 원문 링크를 반드시 함께 저장해 출처를 밝힘.

---

## 7. 화면 설계 (와이어프레임)

```
┌───────────────────────────────────────────────────────────────┐
│ 🗓 정책·경제 이벤트 캘린더        마지막 갱신: 07-20 07:00  [↻ 갱신] │
├───────────────────────────────────────────────────────────────┤
│ [◀] 2026년 7월 [▶]   [월][주][목록]   🔍검색  [카테고리▾][중요도▾] │
├───────────────────────────────────────────────────────────────┤
│  일    월    화    수    목    금    토                          │
│              1     2     3     4                              │
│                   ●CPI  ●고용                                  │
│  5     6     7     8     9    10    11                        │
│                        ●금통위                                 │
│                         ★★★                                   │
├───────────────────────────────────────────────────────────────┤
│ ▼ 이벤트 목록                                    [CSV][ICS]     │
│ 날짜      D-day  분류     기관       이벤트명        중요 상태   │
│ 07-09     D-11   통화정책 한국은행   금통위          ★★★ 확정   │
│ 07-02     지남   경제지표 통계청     6월 소비자물가  ★★☆ 확정   │
│ 07-15     D-17   부동산   국토부     주택공급 대책   ★★★ 잠정   │
└───────────────────────────────────────────────────────────────┘
```

---

## 8. 기술 스택 (C안 채택)

| 옵션 | 구성 | 장점 | 단점 |
|---|---|---|---|
| **A. 경량 로컬형** | React SPA + 로컬 JSON/IndexedDB, 수집은 Python 스크립트로 별도 실행 | 서버 불필요, 배포 간단 | 자동 갱신 어려움 |
| **B. 풀스택형(권장)** | FastAPI(Python) + SQLite + APScheduler / React + TypeScript + FullCalendar | 자동 갱신·이력·API 모두 가능, 파싱 라이브러리 풍부 | 서버 운영 필요 |
| **C. 서버리스형** | GitHub Actions(cron)로 수집 → JSON을 리포지토리에 커밋 → 정적 사이트가 읽음 | 무료·무운영, 갱신 이력이 git 커밋으로 남음 | 실시간성 낮음, 수동 갱신은 워크플로 수동 실행 |

**채택: C안.** 근거는 세 가지입니다.

1. 운영비와 운영 부담이 0이다. 서버·DB·배포 파이프라인이 없다.
2. 갱신 이력이 git 커밋으로 그냥 남는다. 별도 이력 테이블 없이도 "언제 뭐가 바뀌었나"를 추적할 수 있다.
3. 수동 갱신 요구를 Actions의 `workflow_dispatch` 버튼이 그대로 충족한다. 소스 ID를 입력받아 특정 기관만 다시 긁는 것도 된다.

**감수한 단점:** 실시간성이 없다. GitHub의 예약 실행은 부하에 따라 수십 분 늦을 수 있어, 발표 직후 몇 분 내 반영이 필요한 용도에는 맞지 않는다. 이 프로젝트는 "며칠 뒤 일정"을 보는 도구라 문제되지 않는다고 판단했다.

---

## 9. AI 에이전트 팀 구성 (개발 진행 방식)

개발을 병렬화한다면 다음처럼 역할을 나누는 것을 제안합니다.

| 에이전트 | 담당 | 산출물 |
|---|---|---|
| **Source Scout** | 각 기관 사이트/API 조사, 엔드포인트·응답 구조·이용약관 확인 | 소스 조사 리포트, 샘플 응답 |
| **Adapter Dev** | 소스별 Adapter 구현 (fetch/parse/normalize) | 어댑터 코드 + 단위 테스트 |
| **Core Dev** | 스키마, 파이프라인(dedupe/diff), API 서버, 스케줄러 | 백엔드 코드 |
| **Frontend Dev** | 캘린더·테이블·상세 패널·관리 화면 | React 앱 |
| **QA/Validator** | 수집 결과 정합성 검증, 날짜 파싱 엣지케이스, 회귀 테스트 | 테스트 스위트, 버그 리포트 |
| **Doc/Ops** | README, 배포 스크립트, 로그·모니터링 | 문서, CI 설정 |

**진행 순서:** Source Scout → (Core Dev ‖ Adapter Dev) → Frontend Dev → QA → Ops
Adapter는 소스 개수만큼 완전 병렬 가능하므로 여기서 가장 큰 시간 절약이 납니다.

---

## 10. 개발 로드맵

| 단계 | 범위 | 완료 기준 |
|---|---|---|
| **Phase 0** | 스키마 확정, 소스 3개(한국은행·통계청·국회) 조사 | 샘플 데이터 100건 확보 |
| **Phase 1 (MVP)** | 어댑터 3개 + 수동 갱신 + 표 + 월간 캘린더 | 버튼 한 번으로 최신 일정이 화면에 뜸 |
| **Phase 2** | 소스 8개로 확대, 스케줄러, 변경 감지, 필터/검색, ICS 내보내기 | 무인 상태로 1주일 정상 갱신 |
| **Phase 3** | 알림, 즐겨찾기, AI 주간 브리핑, 실제 수치 연동 | — |

---

## 11. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 기관 사이트 개편으로 파서 깨짐 | 수집 실패 | 소스별 격리 + 실패 알림 + 원본 스냅샷 보관, 수동 입력으로 임시 대응 |
| 잠정 일정이 자주 변경됨(특히 부동산 대책) | 잘못된 정보 노출 | `status: TENTATIVE` 명시, 변경 이력 노출, 원문 링크 병기 |
| 크롤링 차단 / 이용약관 위반 | 서비스 중단·법적 이슈 | OpenAPI 우선, robots.txt 준수, 요청 간격 확보, 재배포 시 출처 표기 |
| 날짜 표기 형식 제각각 | 파싱 오류 | 정규화 유틸 + 형식별 테스트 케이스 축적 |
| 중복 이벤트 | 캘린더 지저분 | `dedupe_key` 설계 + 유사도 기반 후보 병합 제안 UI |

---

## 12. 구현하며 바뀐 결정 (v0.1 → v0.2)

설계와 실제 구현이 갈린 지점입니다. 코드를 읽다 설계서와 다르면 이 절이 맞습니다.

### 12.1 데이터 위치를 `data/` → `docs/data/` 로 이동
GitHub Pages가 `/docs` 폴더만 서빙하므로, 데이터가 `data/`에 있으면 프론트엔드가 읽지 못한다.
데이터를 `docs/data/`에 두어 복사 단계 자체를 없앴다. 저장소의 단일 진실 원천도 그대로 이 폴더다.

### 12.2 dedupe_key 를 '날짜' → '연-월' 단위로 변경
설계 초안은 키에 날짜를 통째로 넣었는데, 이러면 발표일이 8/27 → 8/28로 하루 밀렸을 때
**신규 1건 + 취소 1건**으로 잡힌다. 실제로는 "일정 변경"이라는 단일 사건이다.
키를 연-월까지만 쓰도록 바꿔 `RESCHEDULED`로 잡히게 했다.
같은 달에 같은 이름의 회의가 여러 번 열리는 소스(국회 본회의 등)는 `day_level_identity=True`로 날짜까지 포함한다.

### 12.3 사라진 이벤트를 삭제하지 않고 `CANCELLED` 로 표시
설계에는 취소 판정만 있었는데, 파서가 깨져 응답이 비면 정상 일정이 대량 삭제될 수 있다.
삭제 대신 상태만 바꾸고, 이번 실행에서 **실제로 수집한 소스**의 이벤트만 취소 판정 대상으로 좁혔다.
(한국은행만 갱신했는데 통계청 일정이 사라지면 안 된다.)

### 12.4 `rules` 소스 신설 — 규칙 기반 잠정 일정
공식 공표일정을 긁기 전에도 달력이 비어 보이지 않도록, 공표 주기가 규칙적인 지표
(소비자물가·고용동향 등)를 규칙으로 생성한다. 전부 `status=TENTATIVE`이며
공식 어댑터가 같은 이벤트를 `CONFIRMED`로 가져오면 그쪽이 이긴다.

### 12.5 미검증 어댑터는 `optional=True` 로 격리
실제 응답으로 검증하지 못한 어댑터(`bok`, `assembly`, `holiday`)는 실패해도
전체 수집을 실패시키지 않는다. 검증 후 `False`로 바꾼다.

### 12.6 런타임에서 LLM 을 배제
AI 에이전트는 **개발 단계**에서만 쓴다(Claude Code 서브에이전트, Max 구독 범위 내).
Actions에서 LLM을 호출하면 별도 API 과금이 생기고 수집 결과가 비결정적이 되므로,
수집기는 순수 파서로 유지한다.

---

## 13. 남은 작업

| 우선순위 | 항목 |
|---|---|
| 높음 | `bok`·`assembly`·`holiday` 어댑터를 실제 응답으로 검증하고 `optional=False` 전환 |
| 높음 | `rules`의 공표 주기 값을 실제 공표일정과 대조해 확정 |
| 중간 | 통계청·기재부·국토부 어댑터 추가 (Phase 2) |
| 중간 | 알림 채널 연결 (Slack 웹훅이 가장 간단) |
| 낮음 | 발표 후 실제 수치 자동 채우기, AI 주간 브리핑 |
